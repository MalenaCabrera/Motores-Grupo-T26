using UnityEngine;

public class ahnose : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // probando
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
